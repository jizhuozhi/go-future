// Code generated by scripts, DO NOT EDIT.

package tuples

import (
	"sync/atomic"

	"github.com/jizhuozhi/go-future"
)
{{range $i := .Tuples}}
{{- $tuple := . }}

// Of{{len .TypeParams}} waits for {{len .TypeParams}} Futures of unrelated types and resolves with a Tuple{{len .TypeParams}}.
//
// It fails fast: the first error observed is propagated and the tuple is left
// at its zero value. Futures that are still pending are not cancelled.
func Of{{len .TypeParams}}[{{range $j := .TypeParams}}{{if $j.NotFirst}}, {{end}}{{$j.Name}}{{end}} any]({{range $j := .FuncParams}}{{if $j.NotFirst}}, {{end}}{{$j.Name}} *future.Future[{{$j.Type}}]{{end}}) *future.Future[{{$tuple.TupleType}}] {
	var done uint32
	p := future.NewPromise[{{$tuple.TupleType}}]()
	c := int32({{len .TypeParams}})
{{range $j := .ResVars}}
	var {{$j.Name}} {{$j.Type}}
{{- end}}

	cb := func(err error) {
		if err != nil {
			if atomic.CompareAndSwapUint32(&done, 0, 1) {
				p.SetSafety({{$tuple.TupleType}}{}, err)
			}
		} else {
			if atomic.AddInt32(&c, -1) == 0 {
				p.SetSafety({{$tuple.TupleType}}{ {{- range $k := $tuple.TupleVals}}{{if $k.NotFirst}}, {{end}}{{$k.Name}}{{end}}}, nil)
			}
		}
	}
	{{- range $j := .SubscribeBlocks}}
	{{$j.Future}}.Subscribe(func(val {{$j.Type}}, err error) {
		{{$j.ResVar}} = val
		cb(err)
	})
{{- end}}

	return p.Future()
}
{{end}}